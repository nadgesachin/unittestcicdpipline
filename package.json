{
  "name": "jest-cicd-poc",
  "version": "1.0.0",
  "description": "POC for Jest unit tests with BitBucket CI/CD",
  "main": "src/index.js",
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.5.0"
  }
} 